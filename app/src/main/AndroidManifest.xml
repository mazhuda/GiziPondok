<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.irvan.gizipondok">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".menu_utama">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Perhitungan"
            android:label="Perhitungan"
            android:theme="@style/AppTheme.ActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu_utama" />
        </activity>
        <activity
            android:name=".Menu_makan"
            android:label="Menu Makan"
            android:theme="@style/AppTheme.ActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu_utama" />
        </activity>
        <activity
            android:name=".menu_2125_ts"
            android:label="Menu Makan 2125 kkal"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.ActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu_2125_ts" />
        </activity>
        <activity
            android:name=".cetak"
            android:label="Cetak"
            android:theme="@style/AppTheme.ActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu_utama" />
        </activity>

    </application>

</manifest>